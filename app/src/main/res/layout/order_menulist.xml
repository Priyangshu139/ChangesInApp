<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".code.order.MainActivity">

    <LinearLayout
        android:id="@+id/mainLayout"
        android:layout_width="500dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:orientation="vertical"
        android:layout_alignParentLeft="true">

        <LinearLayout
            android:id="@+id/menuBar"
            android:layout_width="500dp"
            android:layout_height="50dp"
            android:layout_alignTop="@+id/mainLayout"
            android:background="#FFA500"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Duplicate back button removed - DineOut header has back button -->

            <TextView
                android:id="@+id/textView2"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginLeft="20dp"
                android:gravity="center"
                android:text="Menu"
                android:textColor="#ffffff"
                android:textSize="24sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/cartIcon"
                android:layout_width="60dp"
                android:layout_height="match_parent"
                android:layout_gravity="end"
                android:layout_marginRight="10dp"
                android:adjustViewBounds="true"
                android:clickable="true"
                android:onClick="redirectToCart"
                android:scaleType="fitXY"
                android:background="@drawable/orangecart"/>

        </LinearLayout>

        <!-- Call Hall Manager Button -->
        <Button
                android:id="@+id/callHallManagerButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="10dp"
                android:background="@drawable/mybutton"
                android:drawableLeft="@drawable/ic_call_manager"
                android:drawablePadding="8dp"
                android:onClick="callHallManager"
                android:padding="12dp"
                android:text="Call Hall Manager"
                android:textColor="#ffffff"
                android:textSize="16sp"
                android:textStyle="bold"
                android:gravity="center"/>

        <ListView
            android:id="@+id/simpleListView"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:divider="@color/material_blue_grey_800"
            android:dividerHeight="1dp"
            android:clickable="true"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbarThumbVertical="@drawable/custom_scrollbar_thumb"
            android:scrollbarTrackVertical="@drawable/custom_scrollbar_track"
            android:scrollbarSize="12dp"
            android:scrollbarAlwaysDrawVerticalTrack="true"
            android:fadeScrollbars="false"/>
    </LinearLayout>
</RelativeLayout>